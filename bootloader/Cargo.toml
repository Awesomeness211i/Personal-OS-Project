[package]
name = "bootloader"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "bootloader"
test = false
bench = false
# required-features = ["bootloader"]

# [features]
# bootloader = []

[dependencies]
uefi = { path = "../uefi" }
arch = { path = "../arch" }
acpi = { path = "../acpi" }
